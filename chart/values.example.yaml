image:
  pullPolicy: IfNotPresent
  tag: 1.0.21

ingress:
  hostname: binary-blog.local
  extraHostnames: ["other.hostname.local", "bananas.local"]
  tls: true
  className: "sample"
  annotations:
    a: b
    c: d
